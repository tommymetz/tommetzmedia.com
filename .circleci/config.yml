version: 2.1
jobs:
  test:
    docker:
      - image: cimg/node:20.19.0
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: npm install
      - run:
          name: Run tests
          command: npm test
workflows:
  version: 2
  test-workflow:
    jobs:
      - test
